{
  "name": "nexus-ai-os",
  "version": "0.1.0",
  "private": true,
  "description": "Production-grade AI operating system foundation",
  "packageManager": "npm@10.9.8",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@nexus/core && npm run build --workspace=@nexus/brain && npm run build --workspace=@nexus/api && npm run build --workspace=@nexus/worker && npm run build --workspace=@nexus/web",
    "typecheck": "npm run typecheck --workspace=@nexus/core && npm run typecheck --workspace=@nexus/brain && npm run typecheck --workspace=@nexus/api && npm run typecheck --workspace=@nexus/worker && npm run typecheck --workspace=@nexus/web",
    "dev:web": "npm run dev --workspace=@nexus/web",
    "dev:api": "npm run dev --workspace=@nexus/api",
    "dev:worker": "npm run dev --workspace=@nexus/worker",
    "clean": "npm run clean --workspace=@nexus/core && npm run clean --workspace=@nexus/brain && npm run clean --workspace=@nexus/api && npm run clean --workspace=@nexus/web",
    "test": "npm run test --workspace=@nexus/brain"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "@types/express": "^5.0.3",
    "@types/node": "^22.15.3",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "wrangler": "^4.17.0"
  }
}
